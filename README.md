Description

This project is a Python-based web scraper that uses Playwright to extract news data from the Ekantipur website and save it in a JSON file.

 Features

Scrapes news articles

Extracts title, image URL, and author (if available)

Saves data in structured JSON format

Handles dynamic content using Playwright

Installation
pip install playwright
playwright install

Run the project 
python scraper.py
